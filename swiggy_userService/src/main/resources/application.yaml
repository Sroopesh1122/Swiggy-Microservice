spring:
  application:
    name: swiggy-userService
  datasource:
    url: jdbc:mysql://localhost:3306/swiggy_db_userServiceDB?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 8082
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
FRONTEND_URL: http://localhost:5174/
